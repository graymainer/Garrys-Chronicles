--basically an empty entity. But nothing i can do about it. 
--If i remove it, then source throws a fit and kills the entity, 
--rendering our marker check useless.


ENT.Type = "point"
ENT.DisableDuplicator = true
